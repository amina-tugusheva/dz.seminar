#include <iostream>
using namespace std;
int main()
{
	int n,k=0;
	cin >> n;
	int* a = new int [n];
	for (int i = 0; i < n; i++)
	{
		cin >> a[i];
	}
	for (int i = 0; i < n; i++)
	{
		if ((a[i] <= 10) && (a[i] >= -10))
		{
			cout << i << " ";
		}
	}
	
	delete[] a;
}
