#include <iostream>
#include <iomanip>
using namespace std;
int main()
{
	int k = 0, n;
	float s = 0, x;
	cout << "n = ";
	cin >> n;
	cout << "x = ";
	cin >> x;
	for (int i = k; i < n; i++)
	{
		s += pow(-1, k) * pow(x, k);
	}
	cout << s;
}
