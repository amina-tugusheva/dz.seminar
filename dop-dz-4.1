#include <iostream>
using namespace std;
int main()
{
	int n,k=0;
	cin >> n;
	int* a = new int [n];
	int* a2 = new int[n];
	for (int i = 0; i < n; i++)
	{
		cin >> a[i];
	}
	for (int i = 0; i < n; i++)
	{
		if (a[i] < 0)
		{
			k += 1;
			a2[k] = a[i];
		}
	}
	for (int i = k; i >= 1; i--)
		cout << a2[i] << " ";
	delete[] a2;
	delete[] a;
}
